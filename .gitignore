# Binaries
**/bin/
bin-int/
.idea

# Log files
*.log
*.tlog
*.cache

# Visual Studio files and folder
.vs/
**.sln
**.vcxproj
**.vcxproj.filters
**.vcxproj.user

# Premake-generated GNU Makefiles (always build via `premake5 gmake2`).
# The premake5.lua files are the source of truth; the Makefiles are artifacts.
# (Vendored libraries' own source Makefiles live inside their submodules and
#  are tracked there, so they are unaffected by these rules.)
/Makefile
/shado-opengl-api/Makefile
/shado-editor/Makefile
/sandbox/Makefile
/Shado-script-core/Makefile
*.make

**.csproj
**.csproj.filters
**.csproj.user

**/imgui.ini

shado-editor/DotNet
shado-editor/ScriptCore

Shado-script-core/obj

shado-opengl-api.sln.DotSettings.user
